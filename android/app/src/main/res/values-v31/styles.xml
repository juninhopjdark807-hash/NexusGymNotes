<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Android 12+ (API 31): fundo dark. O ícone do sistema é um
         placeholder transparente porque a máscara circular nativa cortaria
         a arte inferior; o logo completo (asset "icone", 240dp, sem máscara)
         é exibido pelo próprio app logo em seguida. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_placeholder</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimationDuration">300</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/app_background</item>
    </style>
</resources>
